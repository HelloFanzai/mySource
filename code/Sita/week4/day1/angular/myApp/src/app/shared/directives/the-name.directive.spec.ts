import { TheNameDirective } from './the-name.directive';

describe('TheNameDirective', () => {
  it('should create an instance', () => {
    const directive = new TheNameDirective();
    expect(directive).toBeTruthy();
  });
});
